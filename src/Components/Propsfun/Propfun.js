import React from "react";

export default function Propsfun(props){
    return(
        <div>
            <h1>Props {props.name}</h1>
        </div>
    )
}

// Note that the props are been passed in app.js file and we can call as may we can.